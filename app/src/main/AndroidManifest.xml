<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="assignment3.fit4039.monash.mds">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomePage.MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage.MovieDetailActivity" />
        <activity android:name=".CinemaSelectActivity.CinemaSelectActivity" />
        <activity android:name=".CinemaMovieSelect.CinemaDetailActivity" />
        <activity android:name=".MapActivity" />
        <!-- Google Play Services Version -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!-- Android Google Maps API v2 Key -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAtkuaU6TIpV3eOlcEYJoy8pDNhtK7Xf_o" />

        <activity android:name=".SeatSelect.SeatSelectActivity" />
        <activity android:name=".ConfirmOrderActivity" />
        <activity android:name=".HomePage.RegisterActivity" />
        <activity android:name=".HomePage.LoginActivity"></activity>
    </application>

</manifest>